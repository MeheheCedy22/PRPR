#include <stdio.h>

int main()
{
    int a,b;
    scanf("%d %d", &a, &b);
    printf("\nSucet nacitanych cisiel je: %d\n", (a+b));
    printf("Sucin nacitanych cisiel je: %d\n", (a*b));

    return 0;
}